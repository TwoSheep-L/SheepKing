当前文件为自动生成文件,描述了当前项目的关键路径映射关系,请勿手动修改该文件。

这里存放映射路径的数据

/src/main.tsx  // 主入口文件，渲染整个聊天应用
/src/App.tsx  // 应用根组件（集成SSE事件处理，含file_diff文件变更实时展示）
/src/App.module.less  // 应用根组件样式
/src/index.html  // HTML 入口文件

/src/components/Header/index.tsx  // 顶部栏组件，显示标题、连接状态、主题选择器
/src/components/Header/index.module.less  // 顶部栏样式
/src/components/ChatPanel/index.tsx  // 对话面板组件，展示消息列表和输入框
/src/components/ChatPanel/index.module.less  // 对话面板样式
/src/components/MessageItem/index.tsx  // 消息项组件，支持普通Markdown渲染和file_diff类型（DiffView组件）展示
/src/components/MessageItem/index.module.less  // 消息项样式（含file_diff特殊样式）
/src/components/DiffView/index.tsx  // 🔄 文件diff展示组件，GitHub风格：绿色=新增行，红色=删除行，左侧显示行号
/src/components/DiffView/index.module.less  // DiffView样式，支持6种主题配色
/src/components/ConsolePanel/index.tsx  // 控制台面板组件，实时展示后端日志
/src/components/ConsolePanel/index.module.less  // 控制台面板样式
/src/components/AskModal/index.tsx  // 追问弹窗组件，AI 追问用户的弹窗
/src/components/AskModal/index.module.less  // 追问弹窗样式
/src/components/ThemeSelector/index.tsx  // 主题选择器组件，6 个浅色主题切换
/src/components/ThemeSelector/index.module.less  // 主题选择器样式

/src/hooks/useChat.ts  // 聊天逻辑 Hook（新增addDiffMessage方法，由SSE file_diff事件触发添加diff消息）
/src/hooks/useSSE.ts  // SSE 连接 Hook（新增onFileDiff事件处理，监听后端广播的文件变更）
/src/hooks/useTheme.ts  // 主题管理 Hook

/src/types/index.ts  // 类型定义（新增DiffLine、FileDiffData、FileDiffEventData类型，Message增加type和diffData字段）

/src/styles/variables.less  // 样式变量定义
/src/styles/global.less  // 全局样式

/src/assets/vite.svg  // 项目图标资源

/server/index.ts  // 后端服务入口（提供SSE/Chat/UserInput API，新增全局__broadcastDiff函数供FsTool广播文件diff）
/server/package.json  // 后端服务配置文件

/vite.config.ts  // Vite 配置文件（含 proxy 代理配置）
/package.json  // 项目配置文件
/tsconfig.json  // TypeScript 配置文件
/tsconfig.app.json  // TypeScript 应用配置
/tsconfig.node.json  // TypeScript Node 配置
/eslint.config.js  // ESLint 代码检查配置
/.gitignore  // Git 忽略配置
/README.md  // 项目说明文档

/AiConfig/rule/format.md  // AI 配置规则 - 前端格式说明
/AiConfig/rule/format-backend.md  // AI 配置规则 - 后端格式说明

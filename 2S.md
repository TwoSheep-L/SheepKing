当前文件为自动生成文件,描述了当前项目的关键路径映射关系,请勿手动修改该文件。

这里存放映射路径的数据

/package.json  // 项目配置文件
/tsconfig.json  // TypeScript配置文件
/.env  // 环境变量配置
/base.env  // 基础环境变量配置
/readme.md  // 项目说明文档
/chat-server.ts  // Web聊天独立服务（内嵌HTML前端，支持5个浅色系主题切换：浅色默认/浅绿色/浅粉色/浅蓝色/浅紫色）。改进HTTP层所有catch块，输出更详细的错误信息（含堆栈），修复重复catch块的bug
/.gitignore  // Git忽略规则配置

/src/  // 核心源码目录
/src/main.ts  // 主入口文件
/src/main-code.ts  // 主入口代码文件
/src/CreateSkillRun.ts  // 创建技能运行入口

/src/core/  // 核心模块目录
/src/core/BaseAgent.ts  // 基础Agent核心类。改进工具执行异常捕获，新增 formatAgentError 函数，将完整的错误信息（含堆栈、code、cmd等）返回给Agent对话，而不是只取 error.message
/src/core/BaseAgentTool.ts  // 基础Agent工具类
/src/core/BaseMessage.ts  // 基础消息类
/src/core/BaseSkill.ts  // 基础技能类
/src/core/SkillLoader.ts  // 技能加载器
/src/core/llm.ts  // LLM核心模块
/src/core/AgentSessionPool.ts  // Agent会话池管理
/src/core/OrchestratorAgent.ts  // 编排Agent主类
/src/core/OrchestratorAgent-Code.ts  // 编排Agent代码模块

/src/core/Maestro/  // Maestro工作流核心目录
/src/core/Maestro/types.ts  // Maestro类型定义
/src/core/Maestro/MaestroContext.ts  // Maestro上下文管理
/src/core/Maestro/MaestroFlow.ts  // Maestro流程引擎
/src/core/Maestro/MaestroStep.ts  // Maestro步骤定义

/src/skills/  // 技能模块目录
/src/skills/index.ts  // 技能模块索引
/src/skills/AudioGen/  // 音频生成技能
/src/skills/FsSkill/  // 文件操作技能
/src/skills/Generate2SMapSkill/  // 项目路径映射生成技能
/src/skills/ImageGeneraterSkill/  // 图片生成技能
/src/skills/JavascriptGeneraterSkill/  // JavaScript代码生成技能
/src/skills/ReactGeneraterSkill/  // React组件生成技能
/src/skills/ReadCodeSkill/  // 代码阅读技能
/src/skills/RegexSearchSkill/  // 正则搜索技能
/src/skills/RunCommandSkill/  // 命令执行技能
/src/skills/VideoGenerateSkill/  // 视频生成技能

/src/tools/  // 工具模块目录
/src/tools/index.ts  // 工具模块索引
/src/tools/registery.ts  // 工具注册模块
/src/tools/FsTools.ts  // 文件系统工具
/src/tools/AudioGeneration.ts  // 音频生成工具
/src/tools/CallAgentTool.ts  // Agent调用工具。改进子Agent调用异常捕获，新增 formatAgentError 函数，将完整错误信息返回给上层Agent
/src/tools/ImageGeneraterTool.ts  // 图片生成工具
/src/tools/ReadCodeLinesTool.ts  // 代码行读取工具
/src/tools/RegexSearchTool.ts  // 正则搜索工具
/src/tools/RunCommandTool.ts  // 命令执行工具。改进工具执行错误处理，新增 formatError 函数返回详细的错误信息（含code/cmd/stack/stderr/stdout），异步命令增加try-catch避免未处理的Promise rejection
/src/tools/user_input.ts  // 用户输入工具
/src/tools/VideoGeneraterTool.ts  // 视频生成工具

/src/utils/  // 工具函数目录
/src/utils/imageSize.ts  // 纯Node.js工具函数，从base64解析图片宽高（支持PNG/JPEG/WebP），无需额外依赖

/src/Hooks/  // 全局自定义Hook目录
/src/Hooks/useGold.ts  // 统一金币管理Hook（从Redux读取金币余额，提供refreshGold()消费后刷新全局，Header自动同步）

/src/AI/  // AI模型调用体系目录
/src/AI/providerConfig.ts  // 模型配置（定义所有可用模型、提供商映射，每个模型增加capabilities能力标记supportsImage等）
/src/AI/providers/  // AI提供商实现目录
/src/AI/providers/BaseProvider.ts  // 统一的AI提供商抽象接口（AIProvider, ChatParams, StreamChunk, parseOpenAIStream）
/src/AI/providers/ProviderFactory.ts  // AI提供商工厂（注册表模式，按model路由到对应提供商）
/src/AI/providers/DeepSeekProvider.ts  // DeepSeek提供商实现（通过fetch调用，自动注册到工厂）
/src/AI/providers/MimoProvider.ts  // Mimo API提供商实现（api-key认证，自动注册到工厂）

/src/db/  // 数据库目录
/src/db/index.ts  // 数据库索引（导出所有模型，新增了MessageDb站内信模型导出）
/src/db/schemes/  // 数据库模型目录
/src/db/schemes/AiHistory.ts  // AI历史记录模型（content字段改为Schema.Types.Mixed，支持字符串和数组两种多模态格式，移除aiModel的enum限制）
/src/db/schemes/MessageSchema.ts  // 站内信数据模型（支持system/all/personal三种消息类型，readBy字段记录已读用户，支持已读/未读状态管理）

/src/service/  // 后端服务模块目录
/src/service/user.ts  // 用户业务服务（updateUser函数新增金币变动自动检测，当sendGoldMessage=true时自动创建个人站内信通知用户，使用MessageDb直接创建personal类型消息，标题"金币变动通知"，内容包含原值、新值和变动差额）
/src/service/AiImage/  // AI图片后端服务目录
/src/service/AiImage/modelPrice.ts  // 模型金币价格配置文件
/src/service/AiImage/index.ts  // AI图片后端服务（generateImage中间件含金币校验与扣除逻辑）
/src/service/AiChat/  // 统一AI对话服务目录
/src/service/AiChat/index.ts  // 统一AI对话服务（构建消息时根据是否有图片使用多模态数组格式，校验模型是否支持图片，不支持则返回错误提示）
/src/service/AiSplitMaterial/  // AI智能拆分素材后端服务目录（POST /api/aiSplitMaterial/split 接口，接收原图image和描述description，调用豆包API进行图片图层智能拆分，返回多个独立透明PNG素材）
/src/service/AiSplitMaterial/queue.ts  // 任务队列模块（移除QueueTask的n字段和enqueue的n参数，不再控制生成数量；executeTask中去掉sequential_image_generation和max_images逻辑；每次只生成1张图，传入原图作为参考（图生图模式）；豆包API请求体只传model/prompt/size/image/response_format/watermark:false/output_format:"png"；新增imageWidth/imageHeight字段到QueueTask和enqueue参数；新增clampSizeToApiRange()函数将原图尺寸等比缩放到豆包API支持的像素范围（368万~1677万）；executeTask中将原图尺寸传给豆包API的size参数（格式"WxH"））。移除本地文件操作（fs/path/fileURLToPath），改为上传到外网文件服务器（base=http://43.142.255.71:5380，upload端点为/upload/single，文件访问为/files/:filename）；makeTransparentPng流程：下载豆包原图→removeBackground()抠图→用form-data上传透明PNG到文件服务器→解析响应获取外网URL→返回外网URL；上传响应格式兼容处理：支持多种返回格式（url/direct string/filename/path）
/src/service/AiSplitMaterial/index.ts  // 拆分素材服务入口（buildAnalysisSystemPrompt新增outputCount参数控制Mimo分析时拆分成几个图层：>0精确拆分成N层，=0 AI自己决定；genPrompt格式改为"从参考图片中精确提取[图层名称]"语义；buildImageGenPrompt改为提取语义，从参考图中提取元素而非生成新图片；outputCount范围改为1~10；调用enqueue时不再传n参数）。导入parseImageSizeFromBase64解析原图尺寸；buildAnalysisSystemPrompt新增imageWidth/imageHeight参数，genPrompt要求"输出图片尺寸必须为WxH像素，素材内容大小位置与原图一致"；buildImageGenPrompt新增imageWidth/imageHeight参数，根据relativeBounds计算素材在原图中的精确像素位置并写入prompt，要求"素材内容在图片中的大小和位置必须与参考图中的实际大小和位置完全相同"；enqueue调用时传入imageWidth/imageHeight。异步重构：splitMaterial改为立即创建任务返回taskId，后台异步执行；新增runSplitPipeline后台函数：阶段1 Mimo分析→阶段2逐层生图（每完成一层更新数据库）；getSplitDetail保持原样用于前端轮询（返回layers/layerImages渐进式更新）；getMySplitHistory新增statusText/generatedCount/totalCount字段
/src/service/message.ts  // 站内信业务服务层（共9个接口：获取用户列表、未读数量、最近消息、标记已读、全部已读、获取详情、发送站内信、管理员列表、删除站内信；覆盖用户端和管理端完整业务逻辑）

/src/routes/  // 路由目录
/src/routes/AiChat.ts  // AI对话路由（新增GET /api/aichat/models、更新chat路由指向新服务）
/src/routes/AiSplitMaterial.ts  // AI拆分素材路由（split接口改为异步，立即返回taskId）
/src/routes/message.ts  // 站内信API路由（前缀/message，注册了全部9个接口，支持用户端和管理端双端访问）
/src/routes/index.ts  // 路由汇总索引（注册了message路由，完成站内信模块的路由聚合）

/config/routes.ts  // 前端路由配置文件（站内信列表页/message和站内信详情页/message/detail/:id已从Layout路由下移出，变为独立顶层路由，不套用户layout、不显示头部导航栏，与/login同级；新增/ai/splitMaterial路由图标为scissors属AI相关分组；后台站内信管理路由）

/src/types/  // 全局类型定义目录
/src/types/AiChat.d.ts  // AI对话类型定义（新增AiChatParams.images字段、AiModelInfo.capabilities、ContentPart多模态类型，ChatContextItem.content支持string|ContentPart[]）

/src/pages/  // 前端页面目录
/src/pages/AI/  // AI相关页面目录
/src/pages/AI/AiGenerateImage/  // AI图片生成页面目录
/src/pages/AI/AiGenerateImage/index.tsx  // AI图片生成页面（Gallery画廊风格，从本地userGold改为useGold Hook全局管理，消费后调用refreshGold()同步Header）
/src/pages/AI/AiGenerateImage/style.module.less  // AI图片生成页面样式（修复CSS花括号嵌套错误、下拉框宽度从150px加宽至195px、移除重复.headerRow定义、去掉多余margin-top）
/src/pages/AI/AiGenerateImage/options.tsx  // 模型选项配置（含Seedream 5.0 Lite/4.5/4.0三个模型、MODEL_GOLD_MAP金币价格32/35/30、getModelGold()、下拉选项💰金币标签）
/src/pages/AI/SplitMaterial/  // 拆分素材页面目录（AI智能拆分图片图层，双栏布局）
/src/pages/AI/SplitMaterial/index.tsx  // 拆分素材页面（用户上传图片并补充描述，AI智能拆分图片图层为多个独立透明PNG素材，双栏布局：左侧历史拆分记录列表含缩略图和描述，右侧上传区域/描述输入/拆分结果展示）。标签从"每层输出"改为"拆分图层"；InputNumber的max从4改为10；formatter：0显示"自动"，>0显示"N层"；Tooltip改为"控制拆分成几个图层，自动=AI决定，1~10=固定层数"；副标题从"逐层智能生成"改为"逐层从原图中提取/抠出"。图片展示重构：移除antd Image组件导入，改用原生`<img>`标签（解决antd Image在绝对定位下不显示的问题）。新增previewModal状态（visible/url/name）控制当前页面放大预览弹窗；重写handlePreview不再使用window.open，改为设置previewModal状态；页面底部新增全屏遮罩弹窗：半透明黑色遮罩+关闭按钮(✕)+图片名称+原尺寸图片展示（max 95vw/95vh）；点击遮罩/关闭按钮关闭弹窗，点击图片本身不关闭；Eye图标按钮和图片点击都使用handlePreview打开弹窗。异步轮询重构：新增startPolling/stopPolling每2秒调用getSplitDetail；handleSubmit提交后立即开始轮询；pipelinePhase根据轮询结果自动流转analyzing→generating→done/error；generating阶段展示Spin加载中+已完成图层预览；历史列表每项显示状态徽标（Spin=分析中/生成中，✅=完成，❌=失败）及生成进度（如2/5）；任务进行中自动每5秒刷新历史列表
/src/pages/AI/SplitMaterial/style.module.less  // 拆分素材页面样式
/src/pages/AI/Chat/  // AI聊天对话页面目录
/src/pages/AI/Chat/index.tsx  // AI聊天页面（新增图片上传转Base64、图片预览/删除、模型选择器带📷标记支持图片的模型、消息渲染图文混合展示）
/src/pages/AI/Chat/type.d.ts  // 前端聊天类型定义（更新chartMessages支持images数组，chartMsgData适配新后端格式）
/src/pages/AI/Chat/style.module.less  // AI聊天页面样式（新增.msgImages、.previewImages、.inputLeft、.toolBtn等图片相关图片样式）
/src/pages/Profile/  // 个人中心页面目录
/src/pages/Profile/index.tsx  // 个人中心页（含金币展示条 + 信息卡片金币图标，均使用CircleStar星星图标）
/src/pages/Message/  // 站内信页面目录
/src/pages/Message/index.tsx  // 站内信列表页（分页、筛选、标记已读。修复筛选参数bug：之前错误地将filterKey传给type字段，现改为isRead字段正确传参，采用新样式结构）
/src/pages/Message/style.module.less  // 站内信列表页样式升级（渐变背景、卡片式筛选栏、消息卡片hover效果增强、分页区卡片化、响应式媒体查询）
/src/pages/Message/Detail.tsx  // 站内信详情页重构为电脑样式（新增渐变背景、卡片化布局、信封头部图标区、元信息栏含发件人+时间、正文区带label标签、底部消息ID和返回按钮；响应式兼容手机<=640px时自动缩小间距和字号）
/src/pages/Message/detail.module.less  // 详情页样式完全重写（渐变背景、圆角卡片阴影、信封头部56px图标、灰色元信息背景条、预定义pre-wrap正文、响应式媒体查询适配手机）
/src/pages/Admin/  // 管理后台页面目录
/src/pages/Admin/Layout/  // 管理后台布局目录
/src/pages/Admin/Layout/index.tsx  // 管理后台布局（添加站内信管理菜单项）
/src/pages/Admin/Messages/  // 管理后台站内信管理目录
/src/pages/Admin/Messages/index.tsx  // 管理后台站内信管理（表格、发送弹窗、详情弹窗、删除）。发送弹窗增加"个人消息"选项（type=personal），选择后显示用户搜索选择器（从getAllUser获取用户列表），发送时传递receiverId参数
/src/pages/Admin/Messages/style.module.less  // 管理后台站内信样式
/src/pages/Admin/Users/  // 管理后台用户管理目录
/src/pages/Admin/Users/index.tsx  // 管理后台用户管理（编辑用户弹窗中，金币值改变时显示橙色提示条+复选框"发送站内信通知用户金币变动"，勾选后向updateUser接口传递sendGoldMessage=true，实现金币变动自动触发站内信通知）

/src/Layout/  // 布局组件目录
/src/Layout/components/  // 布局子组件目录
/src/Layout/components/Header/  // 顶部导航栏组件目录
/src/Layout/components/Header/index.tsx  // 顶部导航栏（右侧金币显示区域，headerGold + popoverGold两处均使用CircleStar星星图标，显示用户金币数量；添加站内信图标，未读小红点Badge，点击弹出最近消息列表）
/src/Layout/components/Header/style.module.less  // 顶部导航栏样式（新增.headerGold和.headerGoldAmount，金色徽章风格；添加站内信图标和弹窗样式）

/src/services/  // 前端服务模块目录
/src/services/GenAudio.ts  // 音频生成服务
/src/services/AiImage.ts  // AI图片服务（含getMyTasks方法-获取用户历史图片创作记录）
/src/services/chat.ts  // 前端AI聊天服务（新增getModels() API获取模型列表）
/src/services/AiSplitMaterial.ts  // AI拆分素材前端服务（SplitResult接口改为可选字段，异步初始只返回taskId/status；SplitHistoryItem新增statusText/generatedCount/totalCount；submitSplit超时改为30秒）
/src/services/message.ts  // 站内信API前端服务（封装用户端和管理端所有接口调用）

/src/services/types/  // 前端服务类型定义目录
/src/services/types/AiImage.d.ts  // AI图片类型定义（含AiImageHistoryItem、PaginatedResult<T>）
/src/services/types/message.d.ts  // 站内信类型定义（请求/响应类型定义）
/src/services/types/user.d.ts  // 用户类型定义（UpdateUserParams新增sendGoldMessage可选布尔字段，用于控制金币变动时是否发送站内信通知用户）

/src/cli/  // CLI命令行目录
/src/cli/CreateSkill.ts  // 创建技能CLI命令
